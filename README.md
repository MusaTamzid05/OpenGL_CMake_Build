This is a basic build for opengl in cmake.if you add new code than use make command  on the root of the directory.
If you change the CMakeLists.txt than first use "cmake ." and than "make".

Enjoy :) !!

